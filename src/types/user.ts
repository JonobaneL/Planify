export type User = {
  id: string;
  firstName?: string;
  lastName?: string;
  email?: string;
  position?: string;
  username: string;
  avatar?: string;
  location?: string;
  phone?: string;
  role?: string; //change to role type
  type?: string; //change to role type
};
